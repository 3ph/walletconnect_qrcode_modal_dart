name: walletconnect_qrcode_modal_dart
description: QR code modal package for WalletConnect.
version: 0.0.1
repository: https://github.com/nextchapterstudio/walletconnect_qrcode_modal_dart
issue_tracker: https://github.com/nextchapterstudio/walletconnect_qrcode_modal_dart/issues

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  cached_network_image: ^3.2.0
  flutter:
    sdk: flutter
  flutter_cache_manager: ^3.3.0
  json_annotation: ^4.4.0
  qr_flutter: ^4.0.0
  url_launcher: ^6.0.18
  walletconnect_dart: ^0.0.7

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter.
flutter: null
